# Force npm to use exact versions for dependencies
save-exact=true

# Enable legacy peer dependencies to prevent install issues
legacy-peer-deps=true

# Avoid hoisting for better compatibility with Next.js
hoist=false

# Avoid using Yarn's workspaces feature which can cause issues with Next.js
workspaces-update=false
